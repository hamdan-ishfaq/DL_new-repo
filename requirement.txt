# requirements.txt
streamlit
numpy
scipy
tifffile
rasterio
pillow
opencv-python

# --- CRITICAL FIX: PYTORCH CPU-ONLY INSTALL ---
# This forces installation from the PyTorch index, ensuring the CPU version is used.
# Replace the simple 'torch' entry with these lines:
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
